---
local_file: 'demo-audio.mp3'
status: pending
---

# Demo Local File Input

Before running this note, put `demo-audio.mp3` into:

```text
docker/uploads/
```

When n8n runs in Docker, the workflow reads local files from `/home/node/uploads`.
